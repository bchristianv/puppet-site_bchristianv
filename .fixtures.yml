# This file can be used to install module depdencies for unit testing
# See https://github.com/puppetlabs/puppetlabs_spec_helper#using-fixtures for details
---
fixtures:
  forge_modules:
    concat:
      repo: "puppetlabs/concat"
      ref:  "6.0.0"
    firewalld: 
      repo: "crayfishx/firewalld"
      ref:  "3.4.0"
    haproxy:
      repo: "puppetlabs/haproxy"
      ref:  "4.0.0"
    inifile:
      repo: "puppetlabs/inifile"
      ref:  "3.0.0"
    puppet_agent:
      repo: "puppetlabs/puppet_agent"
      ref:  "2.1.2"
    selinux:
      repo: "puppet/selinux"
      ref:  "3.0.0"
    stdlib:
      repo: "puppetlabs/stdlib"
      ref:  "6.0.0"
  repositories:
    yum:
      repo: "https://github.com/bchristianv/puppet_mod-yum.git"
      ref:  "1.0.1"
